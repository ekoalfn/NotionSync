[install]
# Use the official npm registry (the global ~/.npmrc points at a mirror that's
# missing some scoped packages this project depends on).
registry = "https://registry.npmjs.org/"

# 24h supply-chain guard: skip package versions published less than a day ago.
minimumReleaseAge = 86400
# Each entry bypasses the 24h guard for one package — confirm with the user
# before adding any.
minimumReleaseAgeExcludes = ["@lovable.dev/vite-tanstack-config", "@lovable.dev/mcp-js"]
